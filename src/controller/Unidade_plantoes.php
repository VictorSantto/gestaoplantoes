<?php
/*
	/**
	 * Generated by Getz Framework
	 *
	 * @author Mario Sakamoto <mskamot@gmail.com>
	 * @see https://wtag.com.br/getz
	 */
	 
	use lib\getz;
	use src\logic;	 
	use src\model;	 
	
	require_once($_DOCUMENT_ROOT . "/lib/getz/Activator.php");
	
	/*
	 * Filters
	 */
	$where = "";
	
	if ($search != "")
		$where = "unidade_plantoes.data LIKE \"%" . $search . "%\"";	
		
	if ($code != "")
		$where = "unidade_plantoes.id = " . $code;
	
	if (isset($_GET["friendly"]))
		$where = "unidade_plantoes.data = \"" . removeLine($_GET["friendly"]) . "\"";	
		
	$limit = "";	
	
	if ($order != "") {
		$o = explode("<gz>", $order);
		if ($method == "stateReadAll" || $method == "stateCalledAll") {
			$limit = $o[0] . " " . $o[1];
		} else {
			$limit = $o[0] . " " . $o[1] . " LIMIT " . 
					(($position * $itensPerPage) - $itensPerPage) . ", " . $itensPerPage;
		}		
	} else {
		if ($method == "stateReadAll" || $method == "stateCalledAll") {
			$limit = "unidade_plantoes.ordem ASC";	
		} else {
			if ($position > 0 && $itensPerPage > 0) {
				$limit = "unidade_plantoes.id DESC LIMIT " . 
						(($position * $itensPerPage) - $itensPerPage) . ", " . $itensPerPage;	
			}
		}
	}
	
	/**************************************************
	 * Webpage
	 **************************************************/		
	
	/*
	 * Page
	 */
	if ($method == "page") {
		/*
		 * SEO
		 */
		$view->setTitle(ucfirst($screen));
		$view->setDescription("");
		$view->setKeywords("");
		
		$daoFactory->beginTransaction();
		$response["unidade_plantoes"] = $daoFactory->getUnidade_plantoesDao()->read($where, $limit, true);
		$daoFactory->close();
		
		if (isset($_GET["friendly"]))
			$view->setTitle($response["unidade_plantoes"][0]["unidade_plantoes.data"]);

		echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/header.html", $response);
		
		echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . 
				(isset($_GET["friendly"]) ? "/html/unidade_plantoes.html" : "/html/unidade_plantoes.html"), $response);
		
		echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/footer.html", $response);
	}
	
	/**************************************************
	 * Webservice
	 **************************************************/	

	/*
	 * Create
	 */
	else if ($method == "api-create") {
		enableCORS();
		if (isset($_POST["request"])) {
			$request = json_decode($_POST["request"], true);
			// $request[0]["@_PARAM"] = $daoFactory->prepare($request[0]["@_PARAM"]); // Prepare with sql injection.

			$daoFactory->beginTransaction();
			$unidade_plantoes = new model\Unidade_plantoes();
			$unidade_plantoes->setData(logicNull(controllerDateTime($request["unidade_plantoes.data"])));
			$unidade_plantoes->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$unidade_plantoes->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$unidade_plantoes->setUnidade($request["unidade_plantoes.unidade"]);
			
			$resultDao = $daoFactory->getUnidade_plantoesDao()->create($unidade_plantoes);

			if ($resultDao) {
				$daoFactory->commit();
				$response["message"] = "success";
			} else {							
				$daoFactory->rollback();
				$response["message"] = "error";
			}

			$daoFactory->close();
		} else {
			$response["message"] = "error";
		}
		
		echo $view->json($response);
	}
	
	/*
	 * Read
	 */
	else if ($method == "api-read") {
		enableCORS();
		
		if (isset($_POST["request"])) {
			$request = json_decode($_POST["request"], true);
			
			$limit = "unidade_plantoes.id DESC LIMIT " . 
					(($request[0]["page"] * $request[0]["pageSize"]) - 
					$request[0]["pageSize"]) . ", " . $request[0]["pageSize"];	
		}
		
		$daoFactory->beginTransaction();
		$unidade_plantoes = $daoFactory->getUnidade_plantoesDao()->read("", $limit, false);
		$daoFactory->close();
		
		echo $view->json($unidade_plantoes);
	}
	
	/*
	 * Update
	 */
	else if ($method == "api-update") {	
		enableCORS();
		if (isset($_POST["request"])) {
			$request = json_decode($_POST["request"], true);
			// $request[0]["@_PARAM"] = $daoFactory->prepare($request[0]["@_PARAM"]); // Prepare with sql injection.
			
			$unidade_plantoes = new model\Unidade_plantoes();
			$unidade_plantoes->setId($request["unidade_plantoes.id"]);
			$unidade_plantoes->setData(logicNull(controllerDateTime($request["unidade_plantoes.data"])));
			$unidade_plantoes->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$unidade_plantoes->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$unidade_plantoes->setUnidade($request["unidade_plantoes.unidade"]);
			
			$daoFactory->beginTransaction();
			$resultDao = $daoFactory->getUnidade_plantoesDao()->update($unidade_plantoes);

			if ($resultDao) {
				$daoFactory->commit();
				$response["message"] = "success";
			} else {							
				$daoFactory->rollback();
				$response["message"] = "error";
			}

			$daoFactory->close();
		} else {
			$response["message"] = "error";
		}
		
		echo $view->json($response);
	}
	
	/* 
	 * Delete
	 */
	else if ($method == "api-delete") {
		enableCORS();
		if (isset($_POST["request"])) {
			$request = json_decode($_POST["request"], true);
			$request["unidade_plantoes.id"] = $daoFactory->prepare($request["unidade_plantoes.id"]); // Prepare with sql injection.
				
			$result = true;
			$lines = explode("<gz>", $request["unidade_plantoes.id"]);

			$daoFactory->beginTransaction();

			for ($i = 0; $i < sizeof($lines); $i++) {
				$where = "unidade_plantoes.id = " . $lines[$i];
				
				$resultDao = $daoFactory->getUnidade_plantoesDao()->delete($where);
				$result = !$result ? false : (!$resultDao ? false : true);
			}

			if ($result) {
				$daoFactory->commit();
				$response["message"] = "success";
			} else {							
				$daoFactory->rollback();
				$response["message"] = "error";
			}

			$daoFactory->close();
		} else {
			$response["message"] = "error";
		} 

		echo $view->json($response);
	}
	
	else if ($method == "changeOrder") {		
		$result = true;
		$daoFactory->beginTransaction();
		$call = $daoFactory->getUnidade_plantoesDao()->read("unidade_plantoes.id = " . $form[0], "", false);
		$answer = $daoFactory->getUnidade_plantoesDao()->read("unidade_plantoes.id = " . $form[1], "", false);
		$unidade_plantoesDao = $daoFactory->getUnidade_plantoesDao()->read("unidade_plantoes.ordem >= " . $answer[0]["unidade_plantoes.ordem"], "", false);
		if (is_array($unidade_plantoesDao) && sizeof($unidade_plantoesDao) > 0) {
			for ($x = 0; $x < sizeof($unidade_plantoesDao); $x++) {
				$unidade_plantoes = new model\Unidade_plantoes();
				$unidade_plantoes->setId($unidade_plantoesDao[$x]["unidade_plantoes.id"]);
				$unidade_plantoes->setData(logicNull(controllerDateTime($unidade_plantoesDao[$x]["unidade_plantoes.data"])));
			$unidade_plantoes->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$unidade_plantoes->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$unidade_plantoes->setUnidade($unidade_plantoesDao[$x]["unidade_plantoes.unidade"]);
			
				$resultDao = $daoFactory->getUnidade_plantoesDao()->update($unidade_plantoes);			
				$result = !$result ? false : (!$resultDao ? false : true);
			}
			$unidade_plantoes = new model\Unidade_plantoes();
			$unidade_plantoes->setId($call[0]["unidade_plantoes.id"]);
			$unidade_plantoes->setData(logicNull(controllerDateTime($call[0]["unidade_plantoes.data"])));
			$unidade_plantoes->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$unidade_plantoes->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
			$unidade_plantoes->setUnidade($call[0]["unidade_plantoes.unidade"]);
			
			$resultDao = $daoFactory->getUnidade_plantoesDao()->update($unidade_plantoes);			
			$result = !$result ? false : (!$resultDao ? false : true);
		}
		if ($result) {
			$daoFactory->commit();
			$response[0]["message"] = "success";
		} else {							
			$daoFactory->rollback();
			$response[0]["message"] = "error";
		}
		$daoFactory->close();
		echo $darth->json($response);
	}
	
	/**************************************************
	 * System
	 **************************************************/	
	
	else {
		if (!getActiveSession($_ROOT . $_MODULE)) 
			echo "<script>goTo(\"/login/1\");</script>";
		else {
			/*
			 * Create
			 */
			if ($method == "stateCreate") {
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method))
					echo "<script>goTo(\"/login/1\");</script>";	
				else {
					$daoFactory->beginTransaction();
					$response["titles"] = $daoFactory->getTelasDao()->read("telas.identificador = \"" . $screen . "\"", "", true);
					$response["unidades"] = $daoFactory->getUnidadesDao()->read("", "unidades.id ASC", false);
					$daoFactory->close();

					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/menus/menusCST.html", getMenu($daoFactory, $_USER, $screen));
					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/unidade_plantoes/unidade_plantoesCRT.html", $response);
				}
			}

			/*
			 * Read
			 */
			else if ($method == "stateRead" || $method == "stateReadAll") {
				if ($method == "stateReadAll") {
					$method = "stateRead";
				}
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method))
					echo "<script>goTo(\"/login/1\");</script>";	
				else {
					$daoFactory->beginTransaction();
					$response["titles"] = $daoFactory->getTelasDao()->read("telas.identificador = \"" . $screen . "\"", "", true);
					$response["unidade_plantoes"] = $daoFactory->getUnidade_plantoesDao()->read($where, $limit, true);
					if (!is_array($response["unidade_plantoes"])) {
						$response["data_not_found"][0]["value"] = "<p>Não possui registro.</p>";
					}
					$daoFactory->close();

					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/menus/menusCST.html", getMenu($daoFactory, $_USER, $screen));
					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/unidade_plantoes/unidade_plantoesRD.html", $response);
				}
			}

			/*
			 * Update
			 */
			else if ($method == "stateUpdate") {
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method))
					echo "<script>goTo(\"/login/1\");</script>";	
				else {
					$daoFactory->beginTransaction();
					$response["titles"] = $daoFactory->getTelasDao()->read("telas.identificador = \"" . $screen . "\"", "", true);
					$response["unidade_plantoes"] = $daoFactory->getUnidade_plantoesDao()->read($where, "", true);
					$response["unidade_plantoes"][0]["unidade_plantoes.unidades"] = $daoFactory->getUnidadesDao()->read("", "unidades.id ASC", false);
					for ($x = 0; $x < sizeof($response["unidade_plantoes"][0]["unidade_plantoes.unidades"]); $x++) {
						if ($response["unidade_plantoes"][0]["unidade_plantoes.unidades"][$x]["unidades.id"] == 
								$response["unidade_plantoes"][0]["unidade_plantoes.unidade"]) {
							$response["unidade_plantoes"][0]["unidade_plantoes.unidades"][$x]["unidades.selected"] = "selected";
						}
					}
					$daoFactory->close();

					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/menus/menusCST.html", getMenu($daoFactory, $_USER, $screen));
					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/unidade_plantoes/unidade_plantoesUPD.html", $response);
				}
			}

			/*
			 * Called
			 */
			else if ($method == "stateCalled" || $method == "stateCalledAll") {
				if ($method == "stateCalledAll") {
					$method = "stateCalled";
				}
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method))
					echo "<script>goTo(\"/login/1\");</script>";	
				else {
					/*
					 * Insert your foreign key here
					 */
					if ($where != "")
						$where .= " AND unidade_plantoes.@_FOREIGN_KEY = " . $base;
					else 
						$where = "unidade_plantoes.@_FOREIGN_KEY = " . $base;
						
					$daoFactory->beginTransaction();
					$response["titles"] = $daoFactory->getTelasDao()->read("telas.identificador = \"" . $screen . "\"", "", true);
					$response["unidade_plantoes"] = $daoFactory->getUnidade_plantoesDao()->read($where, $limit, true);
					if (!is_array($response["unidade_plantoes"])) {
						$response["data_not_found"][0]["value"] = "<p>Não possui registro.</p>";
					}
					$daoFactory->close();

					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/menus/menusCST.html", getMenu($daoFactory, $_USER, $screen));
					echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/unidade_plantoes/unidade_plantoesCLL.html", $response);
				}
			}

			/*
			 * Screen
			 */
			else if ($method == "screen") {
				if ($base != "") {
					$arrBase = explode("<gz>", $base);
					
					if (sizeof($arrBase) > 1) {
						if ($where != "")
							$where .= " AND unidade_plantoes.@_FOREIGN_KEY = " . $arrBase[1];
						else
							$where = "unidade_plantoes.@_FOREIGN_KEY = " . $arrBase[1];
					}
				}
				
				$limit = "unidade_plantoes.id DESC LIMIT " . (($position * 5) - 5) . ", 5";

				$daoFactory->beginTransaction();
				$response["titles"] = $daoFactory->getTelasDao()->read("telas.identificador = \"" . $screen . "\"", "", true);
				$response["unidade_plantoes"] = $daoFactory->getUnidade_plantoesDao()->read($where, $limit, true);
				$daoFactory->close();

				echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/unidade_plantoes/unidade_plantoesSCR.html", $response) . 
						"<size>" . (is_array($response["unidade_plantoes"]) ? $response["unidade_plantoes"][0]["unidade_plantoes.size"] : 0) . "<theme>455a64";
			}

			/*
			 * Screen handler
			 */
			else if ($method == "screenHandler") {	
				$where = "";

				// Get value from combo
				$cmb = explode("<gz>", $search);

				if ($cmb[1] != "")
					$where = "unidade_plantoes.id = " . $cmb[1];

				$daoFactory->beginTransaction();
				$response["unidade_plantoes"] = $daoFactory->getUnidade_plantoesDao()->comboScr($where);
				$daoFactory->close();

				echo $view->parse($_DOCUMENT_ROOT . $_PACKAGE . "/html/unidade_plantoes/unidade_plantoesCMB.html", $response);
			}

			/*
			 * Create
			 */
			else if ($method == "create") {
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method)) {
					$response["message"] = "permission";
					
					echo $view->json($response);
				} else {
					$unidade_plantoes = new model\Unidade_plantoes();
					$unidade_plantoes->setData(logicNull(controllerDateTime($form[0])));
					$unidade_plantoes->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
					$unidade_plantoes->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
					$unidade_plantoes->setUnidade($form[1]);
					
					$daoFactory->beginTransaction();
					$resultDao = $daoFactory->getUnidade_plantoesDao()->create($unidade_plantoes);

					if ($resultDao) {
						$daoFactory->commit();
						$response["message"] = "success";				
					} else {							
						$daoFactory->rollback();
						$response["message"] = "error";
					}

					$daoFactory->close();

					echo $view->json($response);
				}
			}

			/*
			 * Action update
			 */
			else if ($method == "update") {	
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method)) {
					$response["message"] = "permission";
					
					echo $view->json($response);
				} else {
					$unidade_plantoes = new model\Unidade_plantoes();
					$unidade_plantoes->setId($code);
					$unidade_plantoes->setData(logicNull(controllerDateTime($form[0])));
					$unidade_plantoes->setCadastrado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
					$unidade_plantoes->setModificado(date("Y-m-d H:i:s", (time() - 3600 * 3)));
					$unidade_plantoes->setUnidade($form[1]);
					
					$daoFactory->beginTransaction();
					$resultDao = $daoFactory->getUnidade_plantoesDao()->update($unidade_plantoes);

					if ($resultDao) {
						$daoFactory->commit();
						$response["message"] = "success";
					} else {							
						$daoFactory->rollback();
						$response["message"] = "error";
					}

					$daoFactory->close();

					echo $view->json($response);
				}
			}

			/* 
			 * Action delete
			 */
			else if ($method == "delete") {
				if (!getPermission($_ROOT . $_MODULE, $daoFactory, $screen, $method)) {
					$response["message"] = "permission";
					
					echo $view->json($response);
				} else {
					$result = true;
					$lines = explode("<gz>", $code);

					$daoFactory->beginTransaction();

					for ($i = 1; $i < sizeof($lines); $i++) {
						$where = "unidade_plantoes.id = " . $lines[$i];
						
						$resultDao = $daoFactory->getUnidade_plantoesDao()->delete($where);
						$result = !$result ? false : (!$resultDao ? false : true);
					}

					if ($result) {
						$daoFactory->commit();
						$response["message"] = "success";
					} else {							
						$daoFactory->rollback();
						$response["message"] = "error";
					}

					$daoFactory->close();

					echo $view->json($response);	
				}
			}
		}
	}

?>